#Scene Loader

Export the PreFab "SceneLoader" with all the dependencies.

##How to use it:

SceneLoader has the EventSystem class. To change the scene, call the TriggerOnEndScene (int <- Scene to load) method there.
SceneLoader.Manager you can select the transition and set the time scale.
